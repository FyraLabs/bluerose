# bluerose

bluerose is a daemon wrapper for the bootc "API" surface, allowing consumers to fetch and manage bootc updates.

## Usage

Build and run bluerose as root, then use the D-Bus interface to manage bootc updates.